<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/now"
        android:icon="@drawable/ic_clock_schedule"
        android:title="@string/tab_sleep_now"
        android:checked="true"
        android:color="@color/home"/>

    <item
        android:id="@+id/at_time"
        android:icon="@drawable/ic_edit_calendar"
        android:title="@string/tab_at_time"
        android:color="@color/logger"/>
</menu>